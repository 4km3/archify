---
- name: Install /etc/profile.d/{homebin.sh,gobin.sh}
  tags: bash
  become: yes
  template:
    src: "{{ item }}.j2"
    dest: /etc/profile.d/{{ item }}
    owner: root
    group: root
    mode: 0644
  loop:
    - homebin.sh
    - gobin.sh
