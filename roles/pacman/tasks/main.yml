---
- name: Install prioritized mirrorlist
  tags:
    - pacman
  template:
    src: "{{ item }}.j2"
    dest: /etc/pacman.d/{{ item }}
    owner: root
    group: root
    mode: 0644
  loop:
    - mirrorlist
